[project]
name = "cuda-transformer"
version = "0.1.0"
description = "A CUDA-based transformer model implementation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pybind11>=3.0.4",
    "requests>=2.34.2",
    "torch>=2.12.0",
]
